import { ArtistShell } from "@/components/ui/ArtistShell";

export default function ArtistDashboardLayout({ children }: { children: React.ReactNode }) {
  return <ArtistShell>{children}</ArtistShell>;
}
